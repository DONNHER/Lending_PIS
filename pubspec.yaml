name: capstone_application
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.12.4
  intl_phone_field: ^3.2.0 # or latest version
  http: ^1.2.2
  flutter_dotenv: ^5.1.0
  equatable: ^2.0.8
  dartz: ^0.10.1
  flutter_bloc: ^9.1.1
  get_it: ^7.6.4
  provider: ^6.1.5+1
  image_picker: ^1.1.2
  mobile_scanner: ^4.0.1
  intl: ^0.19.0
  network_info_plus: ^6.1.0
  philippines_rpcmb: ^0.0.3
  file_picker: ^8.1.7
  url_launcher: ^6.2.2
  path_provider: 2.1.5
  pdf: ^3.11.0
  printing: ^5.13.0
  share_plus: ^10.0.2

  shared_preferences: any
dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.4.3
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/env
